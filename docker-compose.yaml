services:
  api:
    build:
      context: ./build
      dockerfile: Dockerfile.php
    environment:
      APP_ENV: development
      DB_HOST: db
      DB_DATABASE: ${DB_DATABASE:-bank}
      DB_USERNAME: ${DB_USERNAME:-bank}
      DB_PASSWORD: ${DB_PASSWORD:-bank}
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
    ports:
      - ${API_PORT:-8080}:80
    volumes:
      - ./php:/var/www/html
      - php_vendor:/var/www/html/vendor
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mariadb:11.4
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root}
      MARIADB_DATABASE: ${DB_DATABASE:-bank}
      MARIADB_USER: ${DB_USERNAME:-bank}
      MARIADB_PASSWORD: ${DB_PASSWORD:-bank}
    volumes:
      - mariadb_data:/var/lib/mysql
      - ./build/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "mariadb-admin ping -h localhost -u$${MARIADB_USER} -p$${MARIADB_PASSWORD} --silent"]
      interval: 5s
      timeout: 3s
      retries: 20

  phpmyadmin:
    image: phpmyadmin:5-apache
    profiles:
      - tools
    ports:
      - ${PHPMYADMIN_PORT:-8081}:80
    environment:
      PMA_HOST: db
      PMA_USER: ${DB_USERNAME:-bank}
      PMA_PASSWORD: ${DB_PASSWORD:-bank}
    depends_on:
      - db

volumes:
  mariadb_data:
  php_vendor:
